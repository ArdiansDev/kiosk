; Pull Electron's prebuilt binary from a mirror instead of github.com.
; Avoids the "missing path.txt" / failed binary download seen behind
; slow networks, proxies, or firewalls that block GitHub releases.
electron_mirror=https://npmmirror.com/mirrors/electron/
